{% extends 'base.html.twig' %}

{% block body %}
    {% include 'header.html.twig' %}
    <main role="main" class="container min-height400">

        {% autoescape false %}
            {{ outputTypeChunk(currentPage, fields, 'header', 'page_') }}
            {{ outputTypeChunk(currentPage, fields, 'description') }}
        {% endautoescape %}

    </main>
{% endblock %}
